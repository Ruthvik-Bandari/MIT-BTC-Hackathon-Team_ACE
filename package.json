{
  "name": "satsguard",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "build": "bun run --filter '*' build",
    "typecheck": "bun run --filter '*' typecheck",
    "lint": "bunx oxlint ."
  },
  "dependencies": {
    "@cogcoin/indexer": "cogcoin/indexer",
    "cogcoin": "^0.0.1"
  }
}
